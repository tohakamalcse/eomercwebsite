version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      DJANGO_SUPERUSER_USERNAME: toha
      DJANGO_SUPERUSER_PASSWORD: 1234
      DJANGO_SUPERUSER_EMAIL: tohakamalcse@gmail.com